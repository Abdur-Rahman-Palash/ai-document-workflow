# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "ai-document-workflow"
slug: "ai-document-workflow"

runtime: typescript

build:
  install: "npm install"

start:
  type: http
  command: "npm run build && PORT=8000 npm start"
